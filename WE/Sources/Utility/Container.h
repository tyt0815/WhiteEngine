#pragma once

#include <vector>
#include <unordered_map>
#include <queue>
#include <deque>

template <typename T>
using TArray = std::vector<T>;

template<typename T>
using TQueue = std::queue<T>;

template<typename T>
using TDeque = std::deque<T>;