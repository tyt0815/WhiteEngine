#include "ShadowMapPassCommon.sh"

void MainVS(
    in FVertexInput Vin,
    out float4 ClipPosition : SV_Position
)
{
    float4 WorldPosition = mul(float4(Vin.LocalPosition, 1.0f), gWorld);
    
    ClipPosition = mul(WorldPosition, gLightViewProj);
};